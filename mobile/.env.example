# Must be your dev machine's LAN IP, not localhost — on a phone, `localhost`
# means the phone itself. Find yours with:  ipconfig getifaddr en0
# The port must match backend/.env (currently 5001).
EXPO_PUBLIC_API_URL=http://192.168.1.42:5001/api
